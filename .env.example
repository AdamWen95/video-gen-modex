# Modex 服务根地址；不要包含 /v1 或 /v1/video/generations。
MODEX_BASE_URL=https://your-modex-api.example.com

# 替换为你的 Modex API Key。
MODEX_API_KEY=replace-with-your-modex-api-key

# 替换为当前账号实际可用的 Seedance 模型标识。
DEFAULT_MODEL=your-seedance-model
